{
  "name": "arhat-management-system",
  "private": true,
  "description": "Vercel/GitHub entry. The Next.js app lives in web/.",
  "scripts": {
    "postinstall": "npm install --prefix web",
    "vercel-build": "npm run vercel-build --prefix web",
    "build": "npm run build --prefix web"
  }
}
